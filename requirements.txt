# ============================================
# Sentinel Connect - Voice Assistant
# ============================================

# LiveKit Agent Dependencies
livekit-agents>=1.0.0
livekit-plugins-deepgram>=0.4.0
livekit-plugins-groq>=0.2.0
livekit-plugins-silero>=0.4.0
livekit-api>=0.6.0

# Groq AI SDK
groq>=0.4.0

# Audio processing
numpy>=1.24.0
scipy>=1.11.0

# Voice Activity Detection
webrtcvad>=2.0.10

# Audio playback (for local dev)
pygame>=2.5.0

# Environment management
python-dotenv>=1.0.0

# Audio file handling
pydub>=0.25.1
soundfile>=0.12.1

# NVIDIA Riva TTS (optional - for local NVIDIA GPUs)
nvidia-riva-client>=2.14.0

# Server dependencies
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9
websockets>=12.0
aiohttp>=3.9.0

# Note: pyaudio is NOT needed for LiveKit agent (it handles audio internally)
# If deploying to Linux/Render, pyaudio can cause build issues
